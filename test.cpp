#include <iostream>
#include <string>
// #include <cstdlib> // Required for itoa
#include <stdio.h>
#include <libc.h>

int main() {
//   int number = 6565;

//   char buffer[20]; // Make sure the buffer is large enough
//     itoa(number, buffer, 10); // Convert to base-10 string
//   std::string res(buffer);   // Create a std::string from the buffer

  std::cout << "The string is: " << res << std::endl;
  return 0;
}
